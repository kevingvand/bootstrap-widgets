- Check for existing table data
- specify table source
    - use a mapper to map any api to a table format
+ Possibility to specify width of a column
+ Filter column
    + search
        + & and | in search and brackets ()
    + filter values
        + reset all filters (disable all button)
        - Enable all filters
        + Max filter Count
        - search in filters
+ Sort asc/desc
    + sort number if column is type number
+ reorder columns
    - Re-init resizing of columns on column reorder
+ show/hide columns
+ pagination
    + load specific item from url
    - scroll to specified item?
+ row click function
- action icons (at end of table)
    + disable / enable actions on row level
    + column visibility, hide actions if no columns
    + column order - account for action cell
- row right click context menu
- style search "x" for clearing input
- hide function for filter row
- full screen mode (somehow?)
- option for saving specific settings in session (see ValiBridge collapsible card)
- Speed improvements for > 100.000 rows